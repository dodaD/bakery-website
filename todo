- read about composables in vue 3

\*\* fix the background image on the hero section

\*\* make all images same ratio

-fix this
const showCart = ref(false);

function addToCart(item) {
shoppingCart.value?.addItemToCart(item);
}
